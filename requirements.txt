# pip alternative to environment.yml (conda is the primary environment - see CLAUDE.md).
# Core runtime
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
pyarrow>=12.0
# App
flask>=3.0
# EDA / notebooks / figures
matplotlib>=3.7
seaborn>=0.12
jupyter>=1.0
# Report generation (optional)
reportlab>=4.0
# Dev (optional)
pytest>=7.4
pytest-cov>=4.1
# Optional library benchmarks (not required to run the project):
# implicit>=0.7
# scikit-surprise>=1.1.4
